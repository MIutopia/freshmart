# FreshMart 项目过程总览

本文按需求确认、架构设计、规则固化、代码实现、验证和后续计划整理项目全过程。代码和提交历史是实现事实来源；规则说明以本目录其他专题文档为准。

## 1. 初始目标

项目题目为“基于 Spring Boot + Vue 的生鲜果蔬配送商城系统的设计与实现”。技术路线确定为 Java 17、Spring Boot 3、Vue 3、MySQL 8，采用前后端分离、模块化单体起步，并预留 AI Gateway 和后续服务拆分边界。

首期网页端按四类业务端组织：用户端、商家端、平台管理后台、平台配送员端。AI 首期只做智能导购和订单查询；商家经营分析先做数据看板，后续再做 AI 分析。

## 2. 需求逐步确认

1. 支持多商家入驻，商家先审核后营业；平台抽取佣金并生成结算记录。
2. 平台统一配送，配送区域为固定区域，配送员为平台自有人员；按商家生成子订单和独立配送任务。
3. 商品按重量计价，市场价和商家价按千克维护，实际重量按克记录；商家价不得超过市场价 5%，合规上浮差额由平台承担。
4. 支付首期支持余额和微信 Native 扫码；本地使用模拟支付或个人收款码人工确认，不接商户号回调和域名回调。
5. 下单预占库存 15 分钟，超时释放；配送费按平台统一规则计算，默认满 59 元免配送，否则 6 元，规则可配置。
6. 售后首期仅缺货和品质问题；运输损坏只能作为品质问题证据。送达后 1 天内申请，必须上传图片并填写描述，由客服和 AI 辅助、客服最终确认。
7. 支持优惠券、满减、会员等级、积分、秒杀，优惠可叠加；秒杀价与普通折扣互斥。多仓库按商家和仓库边界限制，不跨仓、跨区拆单。
8. 支持电子小票、批次追溯、临期 3 天低价促销候选、站内消息和只读开放 API；暂不做小程序和 App，单个图片/视频不超过 30 MB。
9. 首期容量目标为日订单 50 单、并发用户 20 人。

## 3. 架构与协作决策

- 采用 `freshmart_user`、`freshmart_merchant`、`freshmart_delivery`、`freshmart_trade`、`freshmart_log` 五个 Schema，均在本机 MySQL `127.0.0.1:3307`；跨库只用业务 ID，不建跨库外键。
- 认证采用账号密码、BCrypt、Bearer Token、角色和审计日志；本地用户、商家、配送员各 10 个账号，统一测试密码为 `password`，另有管理员账号 `admin-test-01`。
- GitHub 远程仓库为 `https://github.com/MIutopia/freshmart`，已完成 CLI 授权和仓库创建。当前开发继续使用 `feature/auth-rbac-merchant-onboarding`，按用户决定不再为每个功能新建分支；`main` 只接收完整验证版本。
- 每次策略、规则、开发、优化和测试都必须写入 [开发变更日志](开发变更日志.md)，失败结果也保留。

## 4. 已完成实现

### 后端与数据

- 认证、角色权限、商家入驻申请/审核、审计日志。
- 五库数据源配置和初始化脚本，含测试账号、用户地址、余额账户、营销表、仓库和配送表。
- 商品分类、商品、仓库、批次库存、仓库可经营分类、商家分类仓配规则和市场价校验。
- 固定配送区域、管理员派单、骑手接单超时回池、取货、送达和绩效统计。
- 多商家交易单、商家子订单、单商家单仓约束、批次库存预占、模拟扫码支付、支付确认扣库存、费用台账和超时释放。
- 会员等级、积分计算、积分流水；优惠券创建、领取、下单预占、支付核销和超时释放；会员折扣与优惠券优惠快照。
- 价格纯函数、运费、称重差额、仓库边界和媒体大小策略测试。

### 前端与视觉资产

- Vue 3 + Vite 前端壳，提供 `/app`、`/merchant`、`/delivery`、`/admin` 四端入口切换和模块占位展示。
- 已生成 FreshMart Logo、Logo mark 以及购物车、看板、配送、果蔬、定位、订单等 SVG 2D 图标，位于 `outputs/fresh-market-assets/`；尚未接入完整业务页面。

## 5. 提交与验证时间线

| 阶段 | 代表提交 | 内容与验证 |
| --- | --- | --- |
| 架构起步 | `25351c2` | 初始化 Spring Boot/Vue、文档和工程目录 |
| 规则与支付设计 | `9fff938`、`5a845bd`、`a9a7262` | 固化市场价、平台补贴、支付、退款和可配置平台规则 |
| 认证与多库 | `445b247`、`d30ec96`、`1db6e9a` | 认证、商家入驻、测试账号、五库数据源 |
| 本地流程 | `f33243a` | 修正 BCrypt 测试密码；用户本机 18 项测试通过 |
| 商品仓储 | `7699988` | 商品、仓库、批次和仓配分类规则；编译 23 个源文件通过 |
| 配送履约 | `842c2ab`、`41d2ba3`、`91c13d5` | 区域、骑手工作台、派单状态机；用户本机 18 项测试通过 |
| 交易支付 | `090b049`、`d299b61` | 15 分钟预占、模拟支付、扣库存、超时释放 |
| 营销基础 | `5d248ed`、`37d1612`、`cda5321` | 可配置促销、叠加计算器；用户本机 20 项测试通过 |
| 会员与积分 | `36c99d5` | 优惠券/会员接口、积分计算和支付积分流水；编译 35 个源文件通过 |
| 交易营销闭环 | `497a2e9`、`32e0272` | 下单折扣、优惠券预占核销释放、价格快照及日志校正；编译通过 |

## 6. 当前验证状态

- 已确认的用户本机结果：15、18、20 项测试分别在对应阶段通过，失败 0、错误 0、跳过 0。
- 当前代码最近一次 `mvn compile` 已通过，35 个源文件编译成功。
- 当前环境执行 `mvn test`/`mvn clean test` 时，测试编译器无法解析已生成的 `target/classes` 中既有主类，12 个测试源文件未进入执行阶段；该 Windows Maven/Javac 类路径问题已记录，不能视为业务测试通过。
- `git diff --check` 已通过；临时 `work/` Git 元数据和 Maven 辅助目录属于本地工作文件，不纳入业务提交。

## 7. 后续未完成项

### P0：必须完成的交易与履约能力

- 余额支付：余额冻结、扣款、流水、幂等、余额不足和退款回退。
- 售后退款完整链路：缺货/品质申请、图片与描述校验、送达后 1 天窗口、客服审核、AI 建议、退款原路退回、库存是否回补、平台损失和佣金反冲。
- 实际称重结算：按件/按重量/称重补差模式，1.50 元平台误差上限，超限人工审核；当前已有纯规则类，尚未接入订单售后流程。
- 平台规则后台化：配送费、佣金、预占时长、称重误差和售后窗口应从规则表读取并提供管理接口；当前部分能力仍使用环境变量兜底。

### P1：营销与运营

- 积分抵扣及退款时积分回退。
- 秒杀活动的开始结束、限购、并发库存扣减和与普通优惠互斥。
- 商家经营数据看板：销售额、订单量、客单价、销量排行、库存预警和履约指标；目前只有规划和营销基础代码，没有看板接口/页面。
- 佣金结算正式计算、商家结算单和平台成本分摊落账；当前已有费用台账基础，尚未完成完整结算服务。

### P2：用户体验、AI 与开放能力

- 完整 Vue 用户、商家、配送和后台页面，登录、购物车、下单、支付、订单、售后和工作台交互。
- AI Gateway 导购、订单查询、工具白名单、数据脱敏、会话和审计；当前仅有架构文档，未接入 AI 服务。
- 节气/节日个性化 SVG 卡片生成、预览、站内消息发送和审计；视觉 SVG 资产已生成，但业务发送链路未实现。
- 电子小票生成查询、批次追溯查询、临期促销候选和出库批次分配；已有表结构/文档，接口和流程未完整实现。
- 开放 API 的客户端密钥、签名、范围授权、商家边界、访问日志及只读商品/库存/订单/小票/追溯接口。
- 微信 Native 商户号签名验证、异步回调、退款回调和正式支付适配器；按当前决策保留为后续能力。
- Redis、对象存储、监控、限流和生产拆库后的 Outbox/补偿机制；当前仅保留架构边界。

## 8. 推荐推进顺序

1. 先修复本机 Maven/Javac 测试类路径并恢复完整回归测试。
2. 完成余额支付和退款售后，补齐库存、积分、优惠券、佣金的反向流水。
3. 把平台规则从环境变量迁移为后台可配置服务，补齐佣金结算和经营看板接口。
4. 实现电子小票、批次追溯、临期促销和开放 API。
5. 实现四端 Vue 页面，再接入 AI Gateway 和节日 SVG 卡片站内消息。
6. 最后接入真实微信商户号回调、对象存储、Redis 和生产部署；完成全链路验收后再合并 `main` 并推送 GitHub。
