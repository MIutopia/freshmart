spring:
  application:
    name: freshmart-backend
server:
  port: ${SERVER_PORT:8080}
management:
  endpoints:
    web:
      exposure:
        include: health,info
ai:
  gateway:
    base-url: ${AI_GATEWAY_BASE_URL:http://localhost:8090}
